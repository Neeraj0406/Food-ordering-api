const { showServerError, showData } = require("../../../common-modules.js/helper")

const addCity = async (req, res) => {
    try {

        // showData()
    } catch (error) {
        showServerError(res)

    }
}

module.exports